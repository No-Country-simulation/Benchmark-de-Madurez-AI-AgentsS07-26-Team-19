# Runtime dependencies — installados en la imagen de producción
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Rate limiting
slowapi>=0.1.9

# Database
asyncpg>=0.30.0

# HTTP client (PDF service)
httpx>=0.28.0

# Logging
structlog>=24.4.0
